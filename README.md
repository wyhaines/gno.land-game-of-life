# gno.land-game-of-life
This is the UI portion of an implementation of Conway's Game of Life, using a blockchain smart contract platform, gno.land, as a backend for the calculations for each generation.
