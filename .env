# Default values for local dev. Adjust as needed.
VITE_CHAIN_ID=test5
VITE_REMOTE_RPC=https://rpc.test5.gno.land

# Example realm path if your contract is at gno.land/r/shortener.
# Change to your actual package path or set it in the .env.local or environment.
VITE_REALM_PATH=gno.land/r/wyhaines/v2/gameoflife
